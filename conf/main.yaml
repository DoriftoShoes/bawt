---
aws:
  access_key:
  secret_key:

subsystems:
  irrigation:
    enabled: false
  camera:
    enabled: true
  sensor:
    enabled: false
  hvac:
    enabled: false

logging:
    config_file: conf/logging.conf
